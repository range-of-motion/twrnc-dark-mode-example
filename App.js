import React from 'react';
import { SafeAreaView } from 'react-native';
import Settings from './Settings';

export default () => {
  return (
    <SafeAreaView>
      <Settings />
    </SafeAreaView>
  );
};
